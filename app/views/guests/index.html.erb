<h1>All Guests</h1>

<% if session[:user_id].present? %>
<h1> Hi <%= @user.username %>! </h1> <br>
<%= link_to "Add Appearance", new_guest_episode_path %>
<% end %>



<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to "#{guest.name}", guest_path(guest) %></li>
  <% end %>
</ul>
